info:
  name: GET_USUARIO_ESTADO_ID
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/usuarios/estado/:id
  params:
    - name: id
      value: "8"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
