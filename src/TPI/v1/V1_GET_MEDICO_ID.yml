info:
  name: V1_GET_MEDICO_ID
  type: http
  seq: 15

http:
  method: GET
  url: http://localhost:3000/api/v1/medicos/:id
  params:
    - name: id
      value: "2"
      type: path
  body:
    type: json
    data: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
