info:
  name: PATCH_USUARIO_CAMBIAR_ESTADO_ID
  type: http

http:
  method: PATCH
  url: http://localhost:3000/usuarioestado/:id
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​