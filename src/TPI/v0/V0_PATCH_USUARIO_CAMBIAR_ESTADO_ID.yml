info:
  name: V0_PATCH_USUARIO_CAMBIAR_ESTADO_ID
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:3000/api/v0/usuarios/estado/:id
  params:
    - name: id
      value: "1"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
