info:
  name: V0_PATCH_USUARIO_CORREO_ID
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:3000/usuarios/:id
  params:
    - name: id
      value: "17"
      type: path
  body:
    type: json
    data: |-
      {
        "email" : "otronuevo@correo.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
